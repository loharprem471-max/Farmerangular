import { JunctionfilterPipe } from './junctionfilter-pipe';

describe('JunctionfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new JunctionfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
